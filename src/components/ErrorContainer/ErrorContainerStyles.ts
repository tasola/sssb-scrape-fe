const styles = () => ({
  errorContainer: {
    margin: 'auto',
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    '& *': {
      textAlign: 'center',
    },
    '& h4': {
      color: '#bdbdbd',
    },
  },
})

export default styles
