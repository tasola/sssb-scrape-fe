import React from 'react'

import MUICheckbox from '@material-ui/core/Checkbox'

import { Props } from './types'

const Checkbox = (props: Props): JSX.Element => <MUICheckbox {...props} />

export default Checkbox
